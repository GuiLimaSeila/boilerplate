import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    container: {
        flex: 1,
        backgroundColor: "#121015",
        padding: 20,
    },
    button: {
        backgroundColor: "#a7e9c4",
        padding: 15,
        borderRadius: 7,
        alignItems: "center",
        marginTop: 20,
    },
    buttonText: {
        fontSize: 17,
        fontWeight: "bold",
        color: "#0a1034",
    }
});

export default styles;